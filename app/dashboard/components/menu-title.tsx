import { PersonStanding } from "lucide-react";

export default function MenuTitle() {
  return (
    <h4 className="flex items-center">
      <PersonStanding size={40} className="text-primary" /> SupportMe
    </h4>
  );
}
